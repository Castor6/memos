# TASK-20260917-local-ops-entry：本地私有运维入口与历史资料整理

- 模块与关键词：AGENTS、本地运维入口、私有资料、历史归档
- 记录日期：2026-09-17（历史事实，不表示实时进度）

## 背景与目标

用户确认将当前运维资料与旧应用历史归档分开，使用英文目录名称；通过固定本地入口查阅私有维护说明，避免每次全局搜索。实际目录、连接资料和凭据不提交到 GitHub。

## 验收标准

- [x] 当前资料与旧应用历史归档分离，SSH 别名和连接脚本更新且只读连接验证通过。
- [x] 证书文件名和内容保持不变，不重签、不调整服务器证书配置。
- [x] AGENTS.md 提供 LOCAL_OPS.md 入口；本地文件由 Git 本地规则排除，公开差异没有私有目录或主机信息。
- [x] 更新私有维护记录，保留历史记录和恢复资料。

## 实现与验证

- 公开 AGENTS.md 增加固定本地入口约定；实际路径仅存本地排除的 LOCAL_OPS.md。
- 当前私有运维目录与历史归档分离，91 个原文件逐一核对保留位置及哈希；修改文件保留原件，证书与密钥内容未变。更新当前说明、连接脚本和维护记录，归档说明明确历史命令不可直接执行。
- 新 SSH 入口只读验证成功，Nginx 与更新定时器 active；HTTPS 证书校验正常、响应 200。两个连接脚本语法检查通过，没有旧连接路径或别名。
- git check-ignore 确认本地入口排除，git diff --check 通过；公开差异不含私有目录、主机信息或凭据。
- 本次仅维护资料整理和只读服务验证，不修改线上应用或发布流程，无需 Changeset。
