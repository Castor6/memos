# TASK-20260922-upstream-v031-review：上游 v0.31.0 合并价值评估

- 记录日期：2026-09-22
- 模块与关键词：上游、v0.31.0、安全、个人空间、迁移、HTML 报告
- 维护归属：`docs/upstream-v0.31.0-review`，本次报告会话
- 关联来源：[上游 Release](https://github.com/usememos/memos/releases/tag/v0.31.0)

## 背景与目标

用户要求评估上游 v0.31.0 哪些内容值得合入个人定制版，并交付 HTML 报告。本次仅分析和制作报告，不引入应用代码或调整线上实例。

## 验收标准

- [x] 固定本地、远端 main、共同祖先及上游发布提交，避免把已有定制功能当作新增收益。
- [x] 对照源码区分优先移植、适配后引入、按需引入与暂缓项，附来源与验收建议。
- [x] 核查权限、个人空间、数据库迁移、API 与独立发布流程的冲突。
- [x] 交付可独立打开、可检索的中文 HTML 报告，验证内容与布局。

## 讨论结论

以本工作区 `7d6097771bee2d435e7a54c060490c276ade90ee` 为分析基线；2026-09-22 获取的 `origin/main` 为 `4c315d8ae4b5d0324e1d876689424cc9f840eb4e`，本地额外包含剪藏扩展迁入。上游发布提交为 `2b2192d4e153bd04f1d325b60fd880cf00d68b01`，共同祖先为官方 v0.30.0 的 `2036c1ffc1b0a1e1fa6a473738c2a5ef520df67f`。

- 建议先分批引入 SSRF、分享附件、时间保存、编号任务、cron 等修复，再适配资源加固、限流、检索和结构化导出。
- 保留现有 Tiptap、独立标签、个人空间与待办语义。上游编辑器修复针对 CodeMirror；粘贴、标签和导航部分与本地规则不同。
- 上游协作 Spaces 与本地个人空间不是同一模型；Proto 部分字段编号类型或含义冲突，SQLite 上游空间迁移会遗漏本地字段。
- D1/bridge 虽出现在历史提交中，但已于最终 v0.31.0 标签撤回；按最终源码评估。
- 管理员权限扩大、会话撤销与实例访问模式解耦分开评估；不把账户/访问规则调整混作可无条件接受的小修。

## 实现结果

新增 [上游 v0.31.0 HTML 评估报告](../reports/upstream-v0.31.0-review.html)：

- 35 个归类评估项：优先移植 8 项、适配后引入 11 项、按需引入 6 项、暂缓或保留本地 8 项、已有等价或已撤回 2 项。
- 提供关键兼容风险、分批实施建议、每项来源和验收要求；支持搜索、分类筛选和展开源码依据。
- 附录包含 165 个上游独有提交与 143 个合并冲突路径；数字是固定基线的差异统计，不代表逐提交测试或工期。
- 单文件内置样式和交互，无外部字体、脚本、统计或运行时依赖；可以离线阅读。外部源码链接需联网，Fork 源码链接固定到本次远端 main 快照。

## 验证事实与边界

2026-09-22：

- 读取上游 Release、GitHub Release API、官方 Go 漏洞条目与 Git 对象。正式发布时间为 `2026-09-19T16:50:57Z`，北京时间为 2026-09-20 00:50:57。
- `git rev-list` 和差异统计：上游独有 165 个提交，本地独有 35 个提交；上游变更 1,113 个文件，双方修改路径交集 153 个。统计含重命名、生成输出及语言文件。
- 执行 `git merge-tree --write-tree --name-only --no-messages HEAD refs/upstream-review/v0.31.0`，返回存在冲突、143 个路径。只生成 Git 合并对象，未改变索引或业务代码；没有执行实际 merge/cherry-pick。
- 执行 SQLite `:memory:` 单脚本迁移探针：用本地 `LATEST.sql` 建库，插入 `space=company`、`is_todo=1` 的笔记，再执行固定上游提交的 `0.31/03__multi_spaces.sql`。确认 `space` 与 `is_todo` 列删除，新增 `space_id` 为 NULL。未运行完整升级链或 MySQL/PostgreSQL 迁移，未触碰真实数据库。
- 检查报告中 111 个源码/提交引用在对应 Git 对象存在，行号未越界；35 张评估卡与 165 条历史目录数量一致，HTML 锚点与脚本语法检查通过。没有逐一验证远端链接的 HTTP 可用性。
- Codex 内置浏览器检查 1440×900、430×739、430×932；没有页面水平溢出、没有移动常驻左侧栏，手机卡片无内容横向溢出。验证分类筛选（8 项优先移植）、关键词搜索（SSRF 1 项）、无匹配状态、清空恢复、展开源码与提交附录检索。控制台未报告错误或警告。检查结束恢复浏览器视口覆盖。
- 文档空白检查通过；未运行应用 Go/Vitest/lint/build、漏洞可达性扫描、完整数据库升级或生产验证。报告中均注明后续移植所需的检查。

## 未覆盖范围与后续建议

本次不实际移植、发布或部署；报告中的实施批次是建议，不代表自动执行承诺。

页面尺寸验证不代表 iPhone/Safari、真实键盘、PWA、触摸行为验证。打印按钮存在，但未执行打印输出或 PDF 版式验收。报告生成、链接检查和内存探针脚本保存在本机忽略的 `tmp/upstream-review/`，不纳入应用交付。
