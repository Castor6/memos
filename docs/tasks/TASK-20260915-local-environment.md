# TASK-20260915-local-environment：协作记录与本地验证环境初始化

- 状态：已完成
- 部署状态：不涉及部署
- 模块与关键词：开发环境、按需检索、SQLite、宽屏、窄屏、iPhone 15 Pro Max、内置浏览器
- 关联任务 / 上游来源：官方 v0.30.0 基线，本次为个人开发工具与文档
- 交付：[PR #1](https://github.com/Castor6/memos/pull/1)，与个人仓库入口整理一起提交，已合并（`2bf6e786`）
- 最后更新：2026-09-15

## 背景与目标

用户维护个人 Memos Fork，后续优先定制需求，也会按需引入上游修复或功能。
需要保存讨论结论、验收标准与修复结果，同时控制每次任务的上下文开销；助手改完代码后应能自行完成本地功能验证。

## 验收标准

- [x] 有简短入口、可搜索任务索引和模板，明确按需读取历史。
- [x] 有一条命令准备依赖，一条命令启动本地前后端。
- [x] 使用独立测试数据和账号，覆盖中文、标签、Markdown、长文本和图片。
- [x] 能停止、重启并保留数据；能安全重置，运行中拒绝重置。
- [x] 实际执行前端检查/测试/构建和相关后端检查，记录真实结果。
- [x] 内置浏览器完成宽屏与窄屏页面检查，操作保存并刷新确认。

## 讨论结论

- 长期规则保持简短；索引只存定位信息，任务详情按编号、关键词或模块检索，不全量加载。
- 助手负责维护记录；重要长期结论提炼到协作规则或模块文档。
- 默认用内置浏览器。初始检查为 1440×900 与 390×844；用户后续明确手机是 iPhone 15 Pro Max，手机验收改用 430px 宽度、739/932 两种高度，详见下文补充。无需连接日常 Chrome。
- 真实 iPhone/Safari、软键盘、触摸与 PWA 行为按需真机确认。
- 本地开发使用与线上相同的 SQLite 驱动及独立虚构数据，保留现有应用逻辑与依赖版本。
- 此阶段建立开发与验证基础；自动发布、容器升级链路、服务器备份维护另行处理。

## 实现结果

- 新增文档索引、任务模板、当前任务和本地开发说明；更新 AGENTS.md 与 README 导航。
- 新增 `.node-version`、`scripts/dev.sh` 和 `scripts/dev.py`：准备、启动、停止清理、样例初始化、归档重置和检查入口。
- 使用现有 HTTP API 初始化样例，不修改应用认证逻辑、数据库结构或生成代码。

## 验证结果

验证日期：2026-09-15。源码基线为 `cde498bf` 加本次工作区修改；应用源码与官方 `v0.30.0` 保持一致。环境验收时尚未提交，后续通过上面的 PR 交付；未部署。

| 检查 | 实际结果 |
| --- | --- |
| `./scripts/dev.sh setup` | 通过：Node 24.21.0、Corepack 调用 pnpm 11.0.1、Go 1.26.2，锁文件安装与 Go 依赖下载完成 |
| `./scripts/dev.sh check frontend` | 通过：TypeScript/Biome 检查 416 文件，Vitest 64 文件 / 255 测试，生产构建通过；构建有现有的大包及插件耗时提示 |
| `./scripts/dev.sh check backend` | 通过：`go test -race ./server/... ./internal/...`，`DRIVER=sqlite go test ./store/...` |
| 首次启动 | 自动创建 1 个测试用户、4 条私有笔记和 1 张 PNG，并核对正文及附件关联 |
| 运行中重置 | 返回非零状态，提示先停止；原数据和服务保留 |
| Ctrl-C 与立即重启 | 前后端监听均退出；修复 TIME_WAIT 导致的端口占用误报后可立即重启 |
| 重启保留 | 浏览器新增并编辑的第 5 条笔记及图片关联仍在；样例未重复创建，SQLite 完整性检查为 `ok` |
| 重置后启动 | 原数据归档到忽略目录，新实例恢复为 1 用户 / 4 笔记 / 1 附件，可重新登录 |
| 监听范围 | 实际监听 `127.0.0.1:8081` 和 `127.0.0.1:3001` |
| 文档与脚本 | 本地文档链接、Bash/Python 语法及 `git diff --check` 通过 |

内置浏览器实测：

- 1440×900：测试账号登录、查看中文样例与图片、创建一条私有验收笔记；文档宽度 1429，无页面水平溢出。
- 390×844：笔记菜单打开、编辑并保存，刷新后文字仍正确；图片预览弹层打开并展示图片；文档宽度 379，无页面水平溢出。
- 图片样例实际加载完成；以上操作后检查控制台，没有 error 记录。
- 停止并重启后通过页面确认编辑结果保留；重置后重新登录。验证结束恢复默认视口并保留本地预览。
- 截图：本机 `tmp/local-dev/evidence/desktop.png`、`mobile.png`；完整检查日志同目录，均被 Git 忽略。

未执行：Docker 容器、MySQL/PostgreSQL 集成、线上部署与真机 iPhone 检查。本次仅初始化文档和本地工具；后续涉及这些范围时按 `AGENTS.md` 执行对应检查。当前环境未准备 Docker，全部驱动检查命令为 `go test -v ./store/...`；容器检查入口为 `./scripts/release_smoke_test.sh`。

## 后续事项

- 按用户后续需求建立任务记录并开发。

## 2026-09-15 补充：按 iPhone 15 Pro Max 校准手机验收

用户提供手机截图，指出手机界面没有左侧侧栏。前次交付展示的是桌面截图，未直观呈现已做过的窄屏检查。

验收标准：430px 宽度时，常驻左侧导航及日历/标签栏隐藏，顶部菜单可操作，无页面水平溢出；同时覆盖较短浏览器可视区和全高布局。

- 核对布局代码：`RootLayout` 在 640px 起显示常驻导航，`MainLayout` 在 768px 起显示日历/标签栏。现有逻辑与手机截图相符。
- 内置浏览器实测 430×932、430×739：均显示顶部 Memos 和菜单，无常驻左栏；页面宽度为 419px（余下为桌面浏览器滚动条空间），没有水平溢出。
- 430×739 下打开顶部菜单，搜索、日历和标签抽屉显示完整，可正常关闭。
- 更新长期规则与开发说明中的手机验收尺寸，保留先前 390×844 的历史验证事实。本次只更新验收约定，没有修改应用布局代码。
- 截图保留在忽略目录：`tmp/local-dev/evidence/iphone-15-pro-max-full.png` 和 `iphone-15-pro-max-browser.png`。用户提供的私人笔记截图未复制到仓库。
- 视口覆盖已恢复，预览标签保留；文档差异使用 `git diff --check` 验证。
