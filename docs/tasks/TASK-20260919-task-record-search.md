# TASK-20260919-task-record-search：任务记录按需检索与多会话维护

- 记录日期：2026-09-19
- 模块与关键词：Task、索引、检索、多会话、worktree
- 维护归属：本任务会话 / `docs/task-record-search` 分支

## 背景与目标

多个会话新增独立任务时都会修改总索引同一位置，造成不必要的合并冲突。保留独立任务文件，取消逐项登记索引，以按需搜索替代。

## 验收标准

- [x] INDEX.md 仅提供固定检索说明，新任务不再修改共享目录。
- [x] 规则、入口和模板明确单个任务文件的维护归属、关联方式及记录边界。
- [x] 历史任务文件保留，检索示例可执行，文档差异无空白问题。

## 讨论结论

同一任务由一个会话负责记录；其他会话独立开展的工作分别记录并互链，交接后可续写原任务。多会话代码修改使用独立 worktree。总目录若有浏览需求可按需生成，但不提交生成结果。

## 实现结果

2026-09-19：完成 AGENTS.md、开发入口、固定检索说明与任务模板调整，并同步定制文档的检索措辞；不迁移或重写历史任务。

## 验证事实与边界

2026-09-19：实际运行文件名、编号、关键词及行号检索示例，结果符合预期；程序核对修改文档中的本地链接全部存在，原有任务文件全部保留。`git diff --check` 通过。仅修改文档，未运行应用测试。

## 未覆盖范围

未修改已归档的旧微信客服文档及历史任务事实；新规则适用于后续 Memos 工作。独立 worktree 避免共用工作区干扰，不能消除多个分支修改同一业务代码产生的语义冲突。
