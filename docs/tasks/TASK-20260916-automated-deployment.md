# TASK-20260916-automated-deployment：免费镜像仓库与自动部署

- 状态：已完成
- 部署状态：个人版 0.1.0 已部署，定时更新已启用
- 模块与关键词：ACR 个人版、Docker、发布、自动部署、SQLite、备份、恢复
- 关联任务：[CI 与版本 PR](TASK-20260915-release-workflow.md)
- 最后更新：2026-09-16

## 背景与目标

用户要求解释发布与部署的区别，通过浏览器配置并走通自动部署流程；明确选择免费 ACR 个人版。延续既有方案，由 GitHub 构建发布镜像，服务器主动拉取更新。

## 验收标准

- [x] 在免费 ACR 个人版创建私有仓库，不购买付费实例。
- [x] 仅合并版本 PR 才发布确定提交对应的版本镜像，普通合并不部署。
- [x] GitHub 只保存镜像发布凭据，不保存服务器 SSH 私钥；实际服务器地址和运维秘密留在私有维护目录。
- [x] 镜像按摘要固定；拉取失败时旧服务持续运行；更新任务串行执行。
- [x] 更新前停止应用并备份完整数据与配置，验证备份后更新；验证失败有明确恢复路径。
- [x] 在一次真实更新中核对版本、登录、笔记与附件访问，并启用定时更新。
- [x] 更新公开部署说明和私有维护记录，明确已实现、已验证与仍未验证的范围。

## 讨论结论

- 用户已知免费个人版的官方开发测试用途限制，仍明确选择该方案；不创建企业版或支付订单。
- 当前服务器为 Linux amd64，Docker Compose 运行官方 0.30.0，SQLite 与附件在持久化目录，已有 Nginx HTTPS 入口。
- 首次发布基线将使用个人版本号 0.1.0；应用源码基线仍为官方 0.30.0。

## 实现结果

- 已创建深圳免费 ACR 个人版、`castor6` 命名空间及私有 `memos` 仓库，关闭命名空间自动创建仓库。
- 用户自行设置并轮换 Registry 密码，已保存到 GitHub `ACR_PASSWORD`；发布用户名和仓库变量已配置，未写入代码或记录。
- 实现可信版本 PR 来源检查、确定提交构建、容器冒烟测试、不可变版本标签复用和最后推进 stable；普通合并不会发布。
- 实现服务器串行拉取、维护入口检查、停机归档、摘要启动、数据检查和失败恢复，以及 systemd 定时器模板。
- 已创建 RAM 用户 `memos-pull`，并成功绑定自定义策略 `MemosRepositoryPullOnly`，仅允许 `cr:PullRepository` 访问深圳 `castor6/memos`。该用户的 Registry 密码已由用户完成设置并登录服务器 Docker；凭据仅保存在服务器，配置文件权限 600。
- PR #6 已合并；服务器已安装更新器、配置和 systemd 文件，备份原 Nginx 配置后加入维护入口，语法检查与 HTTPS 访问通过。首次更新前保持定时器 disabled/inactive；实际升级和浏览器验证通过后才启用。
- 首个个人镜像 `castor-v0.1.0` 已发布，`stable` 指向同一摘要；实际服务器已完成首次升级。

## 验证结果

- 2026-09-16 已只读核对当前运行版本、提交、Compose、Docker 版本及磁盘空间。
- 根目录 6 项 CI/版本规则测试、发布元数据检查及 Actionlint 通过。
- 10 项更新流程测试和 3 项镜像发布保护测试通过。
- 在实际服务器的隔离临时目录运行全部 12 项部署测试通过，含 GNU tar 真实备份/恢复 SQLite、二进制附件及权限，以及损坏归档拒绝恢复。测试只使用自建临时数据，结束后清理；生产仍为官方 0.30.0。
- 运行 `scripts/deploy/integration-test.py` 完整演练通过：独立 Docker Compose 更新、临时 Nginx 的 HTTPS 503 维护入口、候选容器故意损坏测试附件并触发健康检查失败、自动恢复数据库/附件/旧容器、恢复后登录与 HTTPS 200。演练未重载生产 Nginx，所有测试资源已清理。
- 配置 [PR #6](https://github.com/Castor6/memos/pull/6) 已合并，提交 `3d9253f528110737ec29fab6cd538e6c30154df4`。首次 CI 发现 service 文件末尾空行，修正后 [CI 全部必需检查通过](https://github.com/Castor6/memos/actions/runs/35111656435)。
- 普通 PR #6 合并后的发布工作流按条件跳过。随后 [版本 PR #3](https://github.com/Castor6/memos/pull/3) 的 [CI](https://github.com/Castor6/memos/actions/runs/35112718942) 通过，门禁允许后合并为 `f27396441d80f1e2870c6174776a5dff292d5025`。
- [首次真实发布](https://github.com/Castor6/memos/actions/runs/35113178916) 成功，实际执行新装及从官方 0.30.0 升级的容器冒烟测试；版本标签、提交标签和 `stable` 均为 `sha256:6581722f001f6ab5c6df2c3afacd2f7e9ec45b4c3b717d05cf531f4e649bbb91`。下载的 `image.json` 与工作流输出一致，版本为 `0.1.0`，提交为上述版本 PR 合并提交。
- RAM 只读凭据实际拉取成功，`--dry-run` 验证的版本与摘要均与发布记录一致；免费个人版使用固定 Registry 密码，未创建 AccessKey。
- 23:28 首次生产更新成功：维护入口生效、停机备份与归档比较通过，按确定摘要启动并核对版本、提交、数据库完整性与附件哈希。HTTPS 正常，用户/笔记/附件数量保持 1/19/3。备份和部署记录的实际路径仅记录在私有维护目录。
- Chrome 刷新线上页面后，原登录会话仍有效，私有笔记可见；附件页显示 3 个媒体附件，原图片全部加载成功。未改动线上笔记；此次未重新输入账号密码测试全新登录，也未重复真机 iPhone 验证。
- 23:29 启用 systemd 定时器，回读为 enabled/active，约每 5 分钟检查一次；重复执行输出 `Already running the published digest`，确认相同摘要不会重复停机部署。

## 后续事项

自动异地备份和证书续签不在本次范围内，仍未配置。
