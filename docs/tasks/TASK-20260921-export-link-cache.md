# TASK-20260921-export-link-cache：导出与链接预览及标签交互

- 记录日期：2026-09-21
- 模块与关键词：PDF、链接元数据、flomo、标签弹窗
- 维护归属：本次实现会话，分支 `feat/memo-export-link-cache`
- 关联任务：[此前交互改进](TASK-20260920-memos-todo-alignment.md)

## 背景与目标

基于 main `0369d535`，对齐 MCP 中 memos 标签的四项未完成需求。服务器生成 PDF，点击导出显示进度并自动下载，不再显示预览弹窗；浏览器不再下载字体。链接标题和摘要抓取后持久保存，刷新和重启复用历史结果。

## 验收标准

- [x] flomo 侧栏平铺和树状标签恢复简洁无配色样式；正文及富文本编辑链接均为蓝色。
- [x] 编辑历史笔记点击添加标签不跳顶，候选键盘操作正常。
- [x] 链接元数据后台抓取后持久保存，读取笔记即获得已缓存预览，失败可重试。
- [ ] PDF 在服务器生成，中文可复制，包含图片、表格和完整长文；直接下载、显示进度及失败提示。
- [ ] 完成相关自动检查与桌面、两种手机高度验证。

## 讨论结论

- 单用户场景，PDF 使用基本超时及防重复点击即可；具体服务端依赖选择正在确认。
- 标签跳顶发生在点击添加标签时，尚未选择候选。
- 链接缓存成功后不自动刷新历史标题、摘要；首次未完成可显示占位。

## 实现结果

2026-09-21：

- flomo 侧栏使用独立的标签标记，平铺和树状列表均保持灰色文字、透明背景；富文本链接补齐主题蓝色规则。
- 标签弹窗改用 preventScroll 聚焦；候选定位只修改列表 scrollTop，不再调用会滚动祖先的 scrollIntoView。
- 新增 SQLite、MySQL、PostgreSQL 的 link_metadata 表及增量迁移。按 URL 的 SHA-256 定位，首次成功快照写入后不覆盖，不设置自动失效。
- 后台按分钟分批扫描独立链接，补抓历史笔记，成功缓存跳过，失败后续重试；沿用外链网络限制，支持关闭时取消请求。不在后台改写笔记正文或更新时间。
- 元数据接口先读持久缓存；Memo 新增只读 link_metadata 字段，卡片优先直接展示随笔记返回的快照。首次尚无快照时保留加载状态和失败回退。
- 使用 Changesets CLI 生成中文 minor 发布说明。PDF 服务端运行时选择尚待确认，现有导出流程未改。

## 验证事实与边界

2026-09-21：

- `./scripts/dev.sh check frontend` 通过：TypeScript、Biome、77 个文件共 309 项测试和生产构建；保留已有大 chunk 提示。
- `go test -race ./internal/... ./server/...` 通过，新增后台补抓测试另行定向运行；元数据 API 测试证明重建服务对象后复用快照，后台测试覆盖成功去重和失败重试。
- `go test -v ./store/...` 完成，SQLite 测试通过；MySQL/PostgreSQL 及容器升级用例因本机 Docker 未运行而跳过，不能视为三驱动验证通过。
- Buf format/generate/lint 完成；只保留本次协议相关生成输出，未纳入远程生成器造成的无关改写。
- golangci-lint v2.11.3 检查源码路径通过；修复了新增代码的导入格式及 Goldmark 弃用 API 调用。
- 根目录版本/CI 规则 7 项测试及 `check:release origin/main` 通过。
- 本地 IAB：桌面 1440×900，手机 430×739、430×932。点击历史笔记添加标签前后 scrollY 分别保持为 1309.5、1498.5、1364.5；输入框可用，两种手机尺寸无常驻左侧栏和页面横向溢出，已恢复视口覆盖。截图在会话中展示。
- 读取实际计算样式：flomo 平铺、树状侧栏均为灰色 rgb(145,145,145)、透明背景；富文本链接为蓝色 rgb(118,149,233)。
- 在本地可丢弃数据创建 example.com 链接笔记，确认 API 随笔记返回 Example Domain 快照，刷新页面直接显示卡片。
- 开发时 Proto 重新生成触发 HMR 曾出现 AuthProvider/DOM 错误并重载页面，完整刷新后验收操作正常；不将其描述为生产问题已修复。

## 未覆盖范围与后续建议

- PDF 服务端迁移未实现：待确认 Node.js 运行时及镜像调整后继续本任务。
- 真机 Safari、软键盘和 PWA 未验证；430px 只证明窄屏布局。关闭标签弹窗时曾观察到小幅位置变化，本次修复验证针对用户报告的点击打开即跳顶。
- 仍需 Docker 可用环境执行 `go test -v ./store/...` 和三驱动容器升级检查。
- 未改线上数据、未勾选待办、未部署。
