# TASK-20260926-editor-controls-folding：编辑器分组与折叠编辑

- 记录日期：2026-09-26
- 模块与关键词：编辑器、工具栏、浮动菜单、常用词、下划线、折叠、标签、Tab
- 维护归属：当前会话，工作分支 `feat/editor-controls-folding`
- 关联任务：[笔记与待办需求](TASK-20260916-notes-requirements.md)、[编辑器与待办交互](TASK-20260917-editor-and-todo-experience.md)

## 背景与目标

用户确认将编辑框底部工具整理为常用词、段落、列表、文字、插入和更多，常用词独立置于最前。精简选区菜单，补齐下划线、可编辑折叠与旧语法转换，修复添加标签后 Tab 被默认待办复选框接住的问题。

## 验收标准

- [x] 工具分组及常用词顺序符合方案，窄于 560px 时分成工具和保存两行，无横向滚动。
- [x] 选区菜单与链接弹窗保持原选区，文字格式支持预设；清除样式保留链接及块结构。
- [x] 下划线保存、查看、刷新重新编辑均有效。
- [x] 可新建并编辑折叠标题和正文，完整语法可显式转换，历史单行、多行内容保持兼容。
- [x] 折叠前后继续输入，待办状态和进度保持；标签后的 Tab 进入正文，列表缩进不受影响。
- [x] 前端 lint、测试、构建通过；桌面及两个手机尺寸完成本地验收。

## 讨论结论

- 工具栏保留正文下方；新增下划线，不新增文字颜色或高亮调色板。
- 常用词独立入口置于最左；标签保持顶部，可见范围和保存始终可见。
- 折叠通过菜单创建或完整语法显式转换，编辑时展开，阅读时默认收起。
- 复用当前 Markdown 存储与安全渲染，未知内容保留原文，不更改后端 API 和数据库。

## 实现结果

- 2026-09-26：底部工具整理为常用词、段落、列表、文字、插入及更多。可用宽度小于 560px 时分成两行；430px 下常用词保留文字标签，其余入口精简为图标，菜单内显示名称。
- 新增共享富文本命令控制器，底栏和浮层使用相同的格式操作及状态。通过可映射选区书签保留弹窗目标，取消菜单立即消费书签，防止下一次操作误用旧选区。
- 下划线使用 `<u>` 保存并安全展示，重新加载仍可编辑；清除文字样式保留链接及块结构。编辑冻结期间拒绝迟到的格式和插入操作。
- 新增折叠标题和正文节点，兼容单行、多行及可安全转换的 HTML；特殊符号、实体、代码示例、空正文与异常内部编码均有回归覆盖。编辑标题后换行进入正文，正文末尾下方向键退出折叠，保存快捷键保持原偏好。
- 修复添加标签后向前 Tab 被待办复选框接住的问题。补充仅应用的 minor changeset，更新编辑器模块说明。

## 验证事实与边界

- 2026-09-26：Node 24 执行 `corepack pnpm --dir web lint` 通过；完整 `corepack pnpm --dir web test` 为 94 文件、447 项通过；`corepack pnpm --dir web build` 通过。构建保留现有 CSS `::highlight`、大分块及插件耗时提示。
- 根目录 `corepack pnpm test` 13 项通过；`corepack pnpm check:release origin/main` 通过。实际测试覆盖标签焦点、待办缩进、保存快捷键、菜单取消后重选、常用词、链接选区、清格式、下划线与折叠保存往返、只读命令。
- Codex 内置浏览器使用新建本地可丢弃数据，检查桌面 1440×900、手机 430×739 和 430×932。工具顺序正确，手机两行且无常驻左栏；页面滚动宽度不超视口。文字菜单、下划线阅读、折叠默认收起与点击展开、内嵌待办已验证，未观察到控制台 error。
- 截图保存在忽略目录 `tmp/local-dev/evidence/editor-1440x900.png`、`editor-430x739.png`、`editor-430x932-menu.png`，验收后恢复视口覆盖。
- 浏览器文字输入期间遇到并行本机输入，停止输入和保存的人工端到端操作，改用真实 React/Tiptap 集成测试及只读布局验收；误入本地测试保存流程的样例已归档。没有连接生产数据或执行部署。

## 未覆盖范围与后续建议

- iPhone Safari/PWA 的长按选区、真实软键盘与触摸菜单仍需真机验收；窄屏截图不代表真机结果。
- 历史列表、引用中的折叠或嵌套折叠保留原文只读，避免改写归属；在此类容器中创建新折叠时，插入到最外层当前块之后。独立折叠正文内部的待办可正常编辑与保存。
- 不支持的 HTML、数学公式和脚注继续走原文保留路径，不声称可直接富文本编辑这些结构。
