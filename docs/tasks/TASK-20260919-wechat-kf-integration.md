# TASK-20260919-wechat-kf-integration：微信客服内置到 Memos

- 记录日期：2026-09-19
- 模块与关键词：微信客服、Go、内置模块、持久化队列、迁移、设置、统一发布
- 关联文档：[内置方案](../wechat-kf-integration.md)、[Task 约定调整](TASK-20260919-task-records.md)
- 关联 PR：[Memos #23](https://github.com/Castor6/memos/pull/23)、[旧客服 #1](https://github.com/Castor6/wechat-kf-memos/pull/1)

## 背景与讨论结论

用户希望长期只维护一套 Memos，选择将 Python 微信客服服务迁为 Go 内置功能，而非仅把两个项目放在同一仓库。开发、Task 和发布统一归入 Memos，旧项目保留为迁移参照及切换前维护入口。

Memos 镜像由 GitHub Actions 构建并推送镜像仓库，服务器拉取更新；本机无需先有 Docker 才能走既有发布流程。旧客服 Docker 示例没有实际验收，不能沿用它宣称内置镜像已经通过验证。

## 本次方案调整验收

- [x] 两项目 AGENTS、导航和 PR 范围统一到内置方案，保留用户指定的 ~/Code/... 路径。
- [x] 明确必须保留的行为、状态迁移、切换顺序和验证边界。
- [x] 保留 Task 事实记录模式，不重新引入依赖外部变化的状态字段。
- [x] 文档链接、索引和空白检查通过。

## 第一阶段代码验收

- [x] Go 协议验证官方向量、接收方、时效、签名、填充及异常 XML；API 限制下载大小与主机，拒绝重定向，不泄露响应或凭据。
- [x] 单次处理覆盖已有消息类型、纯正文、独立标签、媒体和聊天记录；独立微信笔记不建占位。
- [x] 真实临时 Memos SQLite 中验证 PRIVATE/个人空间、固定 ID 去重、附件失败后恢复、白名单、用户停用及资源归属检查。
- [x] 核心不注册路由或启动消费者，不把同步调用结果冒充持久化回执。

## 完整内置模块的验收

以下保留完整迁移目标；未勾选项不因第一阶段完成而自动成立。

- [ ] Go 回调校验、官方 API、消息和事件处理与旧实现等价；默认关闭和本人白名单生效。
- [x] 复用 Memos 业务逻辑完成个人空间、PRIVATE、纯正文、独立标签、媒体笔记及附件归属（单次处理核心，临时 SQLite 验证）。
- [ ] 重试、事务队列、固定 ID、回执窗口、最终结果与不确定发送处理有测试证据。
- [ ] 设置界面具备可用配置和管理能力，密钥不回显，桌面/手机验证边界明确。
- [ ] SQLite/MySQL/PostgreSQL 迁移与新装结构一致；旧状态可重复导入，不重复创建笔记或发送回执。
- [ ] 本地与 CI 中实际验证新实现、容器新装/升级及功能关闭时的普通 Memos 行为。
- [ ] 生产切换有私有备份及回退步骤，原账号/内容保留，真实消息验证结果另行记录。

## 实现事实与边界

2026-09-19 用户确认“调整 PR，并在本轮开始实现 Go 内置模块”。本次完成 `internal/wechatkf` 与 Memos 内部保存适配器，沿用既有服务方法进行笔记和附件写入，不使用 PAT 或自调用 HTTP。未修改 Memos 认证机制、公开 RPC、存储结构、前端或发布工作流。

官方加解密测试向量暴露了编码差异：腾讯示例 AES Key 的 Base64 未使用位非零；Go 严格位校验会拒绝。实现采用与官方/旧服务等价的 Base64 解码，并保留字符、长度、签名、接收方和填充校验。测试材料来自腾讯公开示例，不是生产凭据。

持久化队列、回执窗口/重试调度、事件处理、设置界面、三驱动迁移、导入工具和生产切换均不属于已完成范围。客户端与单次处理器暂未接入服务器生命周期；不能据此停用旧服务或变更回调。旧服务功能与测试结果仅作参考，不能替代新实现验证。

## 验证事实与边界

2026-09-19 在本机使用合成消息、临时空 SQLite 和模拟微信 HTTP 响应验证；没有访问生产配置、数据库或微信账号。实际通过 `go test -race ./server/... ./internal/...`、Go 包路径 lint 与暂存源码干净副本的完整 `golangci-lint run`、Node 24 下的 `corepack pnpm test`（7 项）、文档链接/索引/模板检查及 Git 空白检查。暂存区导出的干净源码执行 `go mod tidy -go=1.26.2` 后 go.mod/go.sum 均未变化。PR 后续进展直接查询上述链接。

本机已有忽略目录 `tmp/link-preview-repro` 混用 Go package，裸 `golangci-lint run` 会在该历史临时目录报错；使用仓库实际包路径执行 lint，不修改或删除这些既有材料；随后导出暂存区源码，在不含临时目录的干净副本完成完整 lint，结果 0 issues。无前端、数据库结构或 Docker 流程变更，本次不将界面、容器升级、MySQL/PostgreSQL 或手机收信报告为已验收。
