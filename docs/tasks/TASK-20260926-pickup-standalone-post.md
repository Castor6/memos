# TASK-20260926-pickup-standalone-post：Pick up 独立发帖展示

- 记录日期：2026-09-26
- 模块与关键词：Web Clipper、X、Pick up、空上文
- 维护归属：本会话，工作分支 `fix/pickup-empty-context`
- 关联任务：[独立剪藏编辑页](TASK-20260924-clipper-editor-tab.md)

## 背景与确认结论

用户剪藏自己的独立发帖后，Memos 固定显示空的 `What they put down` 和“上文未加载”。用户确认没有提取到上文时直接按独立发帖整理，不需要缺失提醒；有回复或引用内容时维持原有展示。这是显示规则，不代表新增了对 X 帖子类型的可靠判定。

## 实现结果

- 新剪藏只在上文非空时生成对应章节，保留本人正文、图片、来源及可选背景思考。
- 来源链接统一为“我的原帖”，扩展入口文案覆盖发帖、回复、引用帖。
- 移除无回应对象和无法识别对话区域的泛化提醒；原内容页为空时说明已按独立发帖整理。明确检测到未展开正文、引用不可用或对话断层时继续提示。
- 保留旧 pending 请求的正文重建格式，避免重试改变请求内容；已有笔记和原始快照不批量改写。
- Changeset 仅选择 `memos-web-clipper` patch。

## 验证事实与边界

2026-09-26，基线 `ea23758c`：

- 扩展 lint 通过，38 个测试文件、452 项测试通过，覆盖独立帖、正常上文、原图保留和旧请求兼容。
- 生产构建、类型检查、9 种语言文案检查、实际后台 worker 启动检查通过。Python 打包测试 25 项：24 通过、1 因文件系统大小写能力跳过。
- 根 `corepack pnpm check:release origin/main` 通过；提交前工作区与暂存区空白检查通过。
- 本机全局 pnpm 12 与扩展固定 pnpm 11.10.0 冲突；先运行固定版本的冻结依赖安装，再以临时 PATH 包装器确保嵌套 pnpm 也经 Corepack 调用，同时关闭本次运行的自动重复依赖安装。不修改仓库依赖或全局配置。
- 本次未操作真实 X 登录页或在线 Memos；DOM 与请求行为由自动化测试覆盖，不代表浏览器已安装新版。已有笔记不自动清理占位文字。
