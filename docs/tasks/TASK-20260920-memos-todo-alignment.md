# TASK-20260920-memos-todo-alignment：微信静默消息与笔记交互改进

- 记录日期：2026-09-20
- 模块与关键词：微信客服、flomo、PDF、中文字体、标签键盘选择、右键菜单
- 维护归属：本次需求对齐与实现会话，工作分支 `feat/todo-workflow-polish`
- 关联任务：[移动导航与导出](TASK-20260919-mobile-export-experience.md)、[内置主题](TASK-20260919-builtin-three-themes.md)、[微信客服](TASK-20260919-wechat-kf-integration.md)

## 背景与目标

切换 main 并拉取到 `9332cad2`，通过 Memos MCP 查询 memos 标签下未归档、包含未完成任务的待办，结合代码对齐六项需求。用户随后取消微信回执唤起 PWA，要求完成其余五项。

## 验收标准

- [x] 独立发送的整条文本恰好为 `1` 时，不剪藏、不为该消息生成回执，保留消息去重和游标推进。
- [x] flomo 的编辑、候选、卡片、筛选与侧栏标签采用统一外观并保留自定义色；正文链接采用默认标签色，未勾选框更清晰。
- [x] 直接下载真实 PDF，中文可选择、复制、搜索，保留正文图片、表格、任务状态与完整长文。
- [x] 添加标签支持上下键、回车、Esc，并避免中文输入法确认误触发；新建标签是明确候选。
- [x] 卡片右键复用三点菜单及权限，在鼠标附近显示并避让屏幕边缘；链接、图片、已选文字保留原生菜单。
- [x] 完成相关自动检查、桌面和两种手机高度的本地浏览器验证。

## 讨论结论

- PWA：用户实测微信不把 `webapp://` 文本识别成链接，iOS 快捷指令能唤起其 PWA。随后明确取消需求，不改成功回执链接，也不发布测试页。本机临时验证页仅留在忽略目录。
- 静默忽略严格匹配 `1`，不去除首尾空白；聊天记录里的 `1` 照常保存。此前其他消息已排队的回执沿用原有处理，不因这条消息清空。
- PDF：用户明确允许按需加载 PDF 库和可嵌入中文字体。资源从本站加载，无第三方字体服务；日常浏览不加载导出库与字体。

## 实现结果

2026-09-20：

- 在微信消息处理器的授权与消息标识校验后识别独立文本 `1`，复用已有 Ignored 结果和 runner 完成逻辑；补充处理器与 runner 回归测试，更新模块规则文档。
- 抽取共享标签自定义颜色计算，将编辑器、候选、筛选、平铺和树状侧栏接入；扩展 flomo 标签、正文链接与未勾选框样式。
- 标签搜索增加 combobox/listbox 语义、候选高亮、上下循环、回车选择、Esc 关闭、滚动跟随及输入法保护。
- 新增卡片右键 hook，使用已有菜单和虚拟鼠标锚点，继续由菜单组件处理碰撞、操作权限和关闭行为；触摸长按保留原生行为。
- 使用按需加载的 pdfmake、html-to-pdfmake，把现有导出正文转换为可复制文字的 A4 PDF。正文图片嵌入、非 PNG/JPEG 图片转换、表格分页、页码及任务状态处理均在浏览器完成；生成失败可重试，关闭时释放下载对象 URL。
- 随应用分发 Noto Sans SC 常规、粗体两份原始字体及 SIL 许可证，合计约 16 MiB，仅在导出时读取，PDF 只嵌入所用字形。来源、校验值与字体覆盖边界见 `web/public/fonts/noto-sans-sc/README.md`。
- PDF 弹窗保留正文预览，保存按钮下载 `.pdf`，不再调用系统打印。预览不是最终分页预览。
- 使用根目录 Changesets CLI 创建中文 minor 发布说明，不手动修改版本或 CHANGELOG。

## 验证事实与边界

以下均为 2026-09-20 本地验证事实：

- `go test ./internal/wechatkf ./server/runner/wechatkf` 通过；覆盖恰好 `1`、带空白/换行、其它文本、聊天记录内 `1`，以及 runner 去重、游标推进、无笔记和无新回执。
- `go test -race ./internal/... ./server/...` 完整重跑通过。首次执行在未修改的 `TestConcurrentSSOFirstSignInConvergesOnOneUser` 中报告 `store/user_setting.go:170` 刷新令牌切片的数据竞争；主分支相同实现的独立副本定向运行 23 次未复现。未修改认证行为，不能据重跑通过声称该偶发问题已解决。
- `./scripts/dev.sh check frontend` 通过：TypeScript、Biome、77 个测试文件共 307 项测试、生产构建及差异空白检查。构建仍提示部分 chunk 大于 500 kB，PDF 主库为独立按需 chunk（约 973 kB，gzip 347 kB）。
- Go 改动包及 `./cmd/... ./internal/... ./proto/... ./server/... ./store/...` 全部源码路径使用 golangci-lint v2.11.3 检查通过。默认全目录 lint 首次被本地忽略目录中既有的混合 package 临时文件阻止，因此检查使用显式源码路径，未移动或删除其它任务的临时文件。
- 根目录 `corepack pnpm test` 的 7 项版本/CI 规则测试和 `corepack pnpm check:release origin/main` 通过。
- IAB 使用本地可丢弃数据，检查桌面 1440×900、手机 430×739 与 430×932；手机无常驻左侧栏、无页面横向溢出，标签和 PDF 弹窗可操作，保存按钮可见。结束恢复视口覆盖，控制台未见 error。
- 实际操作验证标签部分搜索、向下切换、回车加入已有标签、Esc 关闭；筛选标签自动继承仍正常。读取渲染样式确认标签和正文链接同为 `rgb(118, 149, 233)`，未勾选框边框为 `rgb(137, 147, 162)`。
- 桌面右键成功打开同一操作菜单；靠近右下角时菜单矩形为 x=1207、y=676、right=1335、bottom=878，保持在 1440×900 视口内。原生菜单保留和只读/归档权限由回归测试覆盖。
- 通过保存按钮实际下载 PDF；pypdf 验证两页、1 张内嵌图片，中文代码、已完成/未完成符号、图片后文字与第 13 段均可提取，没有空字符。两页经 Poppler 渲染并逐页检查，无缺字框、文字裁切或表格溢出。本地证据留在 `tmp/local-dev/evidence/pdf/`。

## 未覆盖范围与后续建议

- 窄屏浏览器检查不代表 iPhone/Safari、真实软键盘、触摸与 PWA；iPhone 保存到文件的实际交互仍需真机验证。
- PDF 使用简体中文地区字体，未保证全部语言、罕见字符或 Emoji 字形；斜体映射同字重，代码字体不是等宽。首次导出需下载约 16 MiB 字体，离线首次导出与低内存手机尚未验证。
- 微信验证使用本地模拟协议与数据库，没有向真实微信发送测试消息，也未改线上待办状态。PWA 唤起不在本次交付范围。
