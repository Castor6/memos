# TASK-20260916-notes-requirements：笔记编辑、个人空间与独立待办需求

- 状态：已实现、已验证，待用户验收（含核心字段迁移与索引，草稿 PR #8）
- 部署状态：未部署
- 模块与关键词：编辑器、标签、Emoji、快捷键、文件、常用词、空间隔离、待办、引用卡片、折叠
- 关联任务：无；本期不包含备份实现
- 最后更新：2026-09-17

## 背景与目标

改善桌面和手机的笔记录入，按个人空间隔离数据，并提供独立于笔记的待办功能。用户于 2026-09-16 确认按讨论结果实现；在独立 worktree 中开发，避免干扰正在进行的部署任务。

## 验收标准

以下为用户已确认的行为要求；未明确的迁移细节按保留历史数据的原则设计。

- [x] 标签可配置 Emoji；笔记和待办都支持标签，固定在编辑框上方单独管理。取消正文输入 `#` 触发标签，不再自动向正文最前面插入标签文本。
- [x] 编辑器采用所见即所得：格式直接呈现，不向用户显示 Markdown 语法标记；图片、视频、文件和笔记引用在正文对应位置呈现。
- [x] 编辑器提供可发现的有序、无序列表操作；增删列表项后自动调整编号，换行自动续接列表。
- [x] 保存和换行快捷键可配置，支持桌面 Enter 保存、Ctrl/Shift + Enter 换行；手机回车换行、按钮保存。
- [x] 个人偏好保存到服务器，桌面与手机共用；手机回车行为是已确认的设备适配规则。
- [x] 粘贴文件在光标位置展示，不在笔记底部集中显示附件列表；图片原位预览，视频/音频原位播放，其他文件显示文件卡片。
- [x] 侧边栏“笔记”下方提供独立“待办”入口；每条待办没有标题，只有一份正文，其中可包含多个可勾选事项，不依赖笔记。按用户示意图显示时间与已完成数/总数，勾选后更新进度。
- [x] 常用词支持用户配置、中英文和前缀搜索，仅通过按钮触发，选中后插入正文；正文输入不自动触发候选。
- [x] 空间仅供个人使用，两个空间之间不能搜索或引用；不包含成员协作需求。
- [x] 项目可用标签表示，从标签下新建笔记时默认带上该标签。
- [x] 可配置笔记列表默认最多展示的字数，超过后点击展开。
- [x] 在正文中搜索并插入同空间笔记的引用卡片。
- [x] 编辑器提供加粗、斜体等常用操作及快捷方式。
- [x] 侧边栏“备忘录”改为“笔记”。

## 已确认的范围与实施决策

- 本期不做定期备份，也不进行备份现状核验；不做截止时间和提醒。
- 待办没有标题，一份正文里含多个勾选事项，入口位于笔记下方；不是从笔记抽取任务。图片示例仅用于确认形态。
- 用户明确批准 Tiptap / ProseMirror 核心依赖。新增标题工具只是正文格式，不增加必填标题字段。
- 标签固定在编辑框上方管理，取代正文输入 `#` 的操作；常用词只从按钮打开，支持中文/英文前缀。
- 图片原位预览、音视频原位播放、其他文件原位卡片；选择、拖入和粘贴共用上传流程。
- 当前选中空间属于浏览器标签页，空间定义和个人偏好保存在服务器；切换空间完整导航，清空旧查询和进行中的响应。
- 历史数据归入默认个人空间。旧标签首次编辑时保留为独立标签，原正文不做破坏性删除；查看旧笔记时，已在顶部标签区展示的正文标签不重复显示，代码/链接/转义文本保持原样。历史附件显示在正文末尾，进入编辑后成为正文块。
- 笔记卡片保存插入时的摘要，点击打开当前原笔记；摘要暂不自动跟随来源更新。
- 历史 HTML、数学公式与脚注以原文保留块安全预览，可编辑其前后内容或整体删除，暂不支持块内部富文本修改。普通段落、列表、代码、表格等可直接编辑。
- 最终方案按 2026-09-17 用户要求调整：空间与待办类型迁移至独立列，增加列表、附件与引用索引；偏好与标签元数据保留现有结构。详见 [存储与索引](../personal-content-storage.md)。

## 隔离与交付边界

- 在 `/Users/castor/Code/memos-notes-requirements`、`feat/notes-workspaces` 分支开发；不修改另一任务使用的 `/Users/castor/Code/memos` 工作目录、分支或服务。
- 本地使用该工作目录下 `tmp/local-dev/` 的可丢弃数据库和附件；测试账号 `local-dev`，页面 `http://127.0.0.1:3001`。
- PR：[所见即所得笔记、个人空间与独立待办 #8](https://github.com/Castor6/memos/pull/8)，面向 `Castor6/memos:main`。本任务不合并版本 PR，不发布镜像，不部署服务器。

## 实现结果

- 所见即所得编辑器：段落/标题、粗体、斜体、删除线、高亮、有序/无序/任务列表、引用、链接、行内代码/代码块、撤销/重做和清除格式。格式工具栏常驻；手机折叠部分工具。
- 行内代码使用 `</>` 图标，代码块使用方框代码图标，保留名称提示，落实用户关于图标难区分的反馈。
- 上传先原位占位，成功替换为服务端 URL；失败可以重试或移除。插入后光标落在块后，继续输入不会替换文件/卡片；保存等待上传完成。
- 草稿按账号、入口、空间隔离，保留正文、独立标签、已上传附件和引用。未完成上传的 blob 地址不会成为可提交内容。
- 独立待办卡片按 AST 统计完成数，不把代码里的复选框算作待办；空待办不能保存。
- 空间隔离覆盖笔记/待办列表、搜索、单条读写、标签元数据、附件列表与引用校验；后台任务仍能处理所有空间。跨空间引用失败在创建笔记之前返回，避免残留半条笔记。
- 账号偏好保存到服务器：桌面 Enter 保存开关、中英文常用词、预览字符数。手机触摸设备 Enter 始终换行，输入法组合期间不保存。
- 已通过根目录 `corepack pnpm changeset` 添加 minor 中文发布说明，不改根版本号和 CHANGELOG。
- 同步修复固定 golangci-lint 2.11.3 对既有七个 Unix 秒变量报告的命名问题，仅改局部变量名，不改变行为。

## 本地验证

- 前端 `pnpm lint`、`pnpm test`（65 文件、266 用例）及生产构建通过。覆盖富文本转换、历史 HTML/公式/脚注/表格保留、文件/引用位置及继续输入、列表续接、文件粘贴、桌面/触摸设备 Enter、输入法保护及历史标签不重复显示。
- `go test -race ./server/... ./internal/...` 通过；新增 API 测试覆盖空间查询、跨空间读写拒绝、引用失败无残留、独立待办查询、标签不被正文覆盖，以及服务器偏好和空间 Emoji 隔离。
- `go test ./store/...` 返回成功，SQLite 实测通过。新增 `TestPersonalSpaceQueries` 纳入三驱动测试入口；本机没有 Docker，MySQL/PostgreSQL 与容器升级用例由 TestContainers 跳过，不能称为本机三驱动实测通过。
- `golangci-lint run --timeout=3m` 通过（0 issues）；`go mod tidy -go=1.26.2` 没有改变模块文件。
- 在 proto 目录执行 `buf generate`（此前接口修改后）、`buf lint`、`buf format` 检查；剔除未涉及接口的生成器版本升级噪音。
- 内置浏览器桌面 1440×900、手机 430×739 与 430×932：没有手机常驻侧栏；导航及筛选抽屉可打开/关闭，待办入口位于笔记下方。
- 页面实测：新建公司空间为空、个人历史数据仍在；标签下新建继承标签；Emoji 保存显示；无序/有序列表选择与续接；待办两项保存、勾选进度从 0/2 到 1/2；文件在正文中间保存且后续文字保留；同空间引用卡片；常用词 `bro` 只筛出 `browserrig`；Shift+Enter 换行、Enter 保存；30 字预览提供展开入口。
- 浏览器开发过程中 HMR 曾出现 React DOM 迁移错误；另修复任务复选框从 undefined 切为受控状态的告警。结束前完整重载并再次新增/勾选待办，新日志没有错误。
- 屏幕尺寸检查不等同于 iPhone/Safari、真实软键盘、触摸及 PWA 验收，仍需真机确认。
- 用户截图反馈的“本地测试/编辑器”旧笔记已在内置浏览器展开复核：顶部显示一次标签，正文不再重复显示，源数据未改写。

## CI 验证

- 提交 `04f27172` 的 [CI](https://github.com/Castor6/memos/actions/runs/35122188656) 全部通过，包括 `validate`、前后端测试和构建、三个驱动升级及发布镜像安装/升级。
- 审计发现原 backend store 的 `&& '' || 'sqlite'` 表达式始终回落到 SQLite；已改为反向条件。完整测试暴露并修正了 MySQL JSON 布尔比较与 PostgreSQL 附件 text payload 转换差异。
- 核对 store 作业日志：`TestPersonalSpaceQueries` 在 SQLite、MySQL、PostgreSQL 各通过一次，最终输出 `PASS: all drivers`，没有因 Docker 缺失跳过。
- 此后仅补充旧标签显示去重，已通过上述 266 项前端测试、lint、构建和页面复核；推送后 PR 自动重跑 CI。

## 交付与待验收

- 真实 iPhone 输入法、软键盘、文件粘贴和播放操作。
- 尚未部署，线上界面不代表此工作目录的实现。

## 2026-09-17：本 PR 数据模型与索引优化

用户要求以长期维护为目标，对核心查询字段及时迁移，不因省事沿用 JSON 扩展。继续在 PR #8 实施，尚未部署。

- [x] 笔记空间、笔记/待办类型、附件空间使用独立列作为唯一数据来源；API 行为保持兼容。
- [x] SQLite/MySQL/PostgreSQL 增量迁移与全新安装结构一致；回填旧 JSON 中的预览数据，旧版无空间数据归入默认空间。
- [x] 针对创建/更新时间、置顶列表、附件关联和引用反向查询添加索引，并验证查询计划。
- [x] 保留正文、标签、附件及引用，重复启动不会重复迁移。
- [x] 用批量数据记录查询与写入成本，通过后端、三驱动及升级 CI；不手动修改生产数据库。

实现：结构版本 0.30.2，新增 memo.space / memo.is_todo / attachment.space 与七个索引；移除旧 JSON 属性与内部 proto 字段，外部 API 不变。SQLite 真实查询计划覆盖四种列表与附件/引用路径；10 万条模拟数据的收益与写入代价见模块文档。SQLite 存储测试、server/internal race tests、golangci-lint 通过。三驱动和容器升级验证结果见下。

本地预览已实际从 0.30.1 自动迁移到 0.30.2；迁移前后 9 条内容的正文 SHA-256、空间、类型、标签一致，3 个附件保留。内置浏览器重载后验证个人/公司空间、原引用卡片和待办完成状态，页面恢复至个人笔记。生产未操作。

最终代码提交 `015943ab` 的 [CI 全部通过](https://github.com/Castor6/memos/actions/runs/35125894821)，包括必需检查 `validate`。store 日志确认 SQLite/MySQL/PostgreSQL 各自执行并通过 `TestPersonalSpaceMigration`、`TestPersonalSpaceFreshIndexes`、`TestPersonalSpaceQueries`，最后输出 `PASS: all drivers`。三驱动旧版升级和发布镜像安装/升级检查通过。首次 CI 暴露的旧样本行尾空格及 MySQL 测试 SQL 的 blob 保留字引用已修正。实现提交验证后仅补充本验收记录和索引文档，按文档规则执行 `git diff --check`。
