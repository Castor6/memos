# TASK-20260916-notes-requirements：笔记编辑、个人空间与独立待办需求

- 状态：进行中（需求已确认，准备实现）
- 部署状态：未部署
- 模块与关键词：编辑器、标签、Emoji、快捷键、文件、常用词、空间隔离、待办、引用卡片、折叠
- 关联任务：无；本期不包含备份实现
- 最后更新：2026-09-16

## 背景与目标

改善桌面和手机的笔记录入，按个人空间隔离数据，并提供独立于笔记的待办功能。用户于 2026-09-16 确认按讨论结果实现；在独立 worktree 中开发，避免干扰正在进行的部署任务。

## 验收标准

以下为用户已确认的行为要求；未明确的迁移细节按保留历史数据的原则设计。

- [ ] 标签可配置 Emoji；笔记和待办都支持标签，固定在编辑框上方单独管理。取消正文输入 `#` 触发标签，不再自动向正文最前面插入标签文本。
- [ ] 编辑器采用所见即所得：格式直接呈现，不向用户显示 Markdown 语法标记；图片、视频、文件和笔记引用在正文对应位置呈现。
- [ ] 编辑器提供可发现的有序、无序列表操作；增删列表项后自动调整编号，换行自动续接列表。
- [ ] 保存和换行快捷键可配置，支持桌面 Enter 保存、Ctrl/Shift + Enter 换行；手机回车换行、按钮保存。
- [ ] 个人偏好保存到服务器，桌面与手机共用；手机回车行为是已确认的设备适配规则。
- [ ] 粘贴文件在光标位置展示，不在笔记底部集中显示附件列表；各文件类型的呈现方式待确认。
- [ ] 侧边栏“笔记”下方提供独立“待办”入口；每条待办没有标题，只有一份正文，其中可包含多个可勾选事项，不依赖笔记。按用户示意图显示时间与已完成数/总数，勾选后更新进度。
- [ ] 常用词支持用户配置、中英文和前缀搜索，仅通过按钮触发，选中后插入正文；正文输入不自动触发候选。
- [ ] 空间仅供个人使用，两个空间之间不能搜索或引用；不包含成员协作需求。
- [ ] 项目可用标签表示，从标签下新建笔记时默认带上该标签。
- [ ] 可配置笔记列表默认最多展示的字数，超过后点击展开。
- [ ] 在正文中搜索并插入同空间笔记的引用卡片。
- [ ] 编辑器提供加粗、斜体等常用操作及快捷方式。
- [ ] 侧边栏“备忘录”改为“笔记”。

## 讨论结论

- 本期排除定期备份，不进行实现或服务器核验；不据此认定已有备份。
- 待办是独立功能，不是从笔记中抽取复选框；本期不做截止时间、提醒。
- 用户明确待办没有标题；此前“待办标题→多个有标题列表”的理解作废。以一张卡片内的多个勾选事项为基本形态，例如“吃饭”“打扫”，初始进度为 0/2。
- 用户明确所见即所得，不显示 Markdown 标记。此前保留源码标记的编辑方式不满足目标；底层存储格式尚未选择。
- 用户明确标签固定在编辑框上方管理。此前向正文开头写入 `#标签` 的方案作废；按独立标签信息设计，历史正文标签如何提取与兼容仍需制定迁移方案。
- 用户授权扩展方便常用的编辑操作；建议首版包含正文/标题、加粗、斜体、删除线、高亮、有序/无序列表、链接、引用段落、行内代码/代码块、清除格式、撤销/重做，以及已提出的文件、常用词和笔记卡片插入。具体工具栏布局为建议，尚未确认。
- 建议桌面直接显示常用格式工具，手机常用按钮外露，其余收进“更多”；顶部标签始终有独立入口。格式工具中的标题是正文格式，不给笔记或待办添加必填标题字段。
- 建议换行命令在待办项末尾创建下一项，在项内拆分内容，空项再次换行退出列表；沿用已确认的桌面/手机保存规则。
- 常用词明确不在正文中自动弹出；中文前缀匹配不默认扩展为拼音匹配。
- 笔记引用确定为卡片；卡片字段和更新行为尚未确定。
- 设置在服务器保存并跨设备共用，不以浏览器本地存储作为唯一来源。
- 待确认：历史笔记迁入哪个默认空间；历史附件列表是否迁移至正文；历史正文标签的迁移与兼容行为。
- 建议但未确认：图片原位预览，视频/音频原位播放，其他文件原位显示文件卡片；文件选择和拖拽也使用相同规则。
- 建议但未确认：空间范围覆盖笔记、待办、标签、文件与草稿，后台请求也校验归属；常用词等个人偏好由账号共用。

## 实施安排与并行边界

- 另一个活跃任务“制定项目沉淀与验证方案”使用 `/Users/castor/Code/memos`，处理发布、部署与验证记录；不在该目录切分支、修改文件或重启其服务。
- 本任务使用 `/Users/castor/Code/memos-notes-requirements` 的独立工作目录与功能分支，提交面向 `Castor6/memos:main` 的 PR；不通过本任务触发版本发布或服务器部署。
- 开发测试使用本工作目录的可丢弃数据库和附件。启动前检查端口，端口冲突时选择独立端口，不终止另一任务的进程。
- 合并前同步 main，重点检查共享的任务索引、Proto、数据库迁移编号及锁文件；按顺序合并 PR，合并后的版本发布仍遵循现有流程。
- 已确认全部产品范围；实施顺序为：空间/独立标签/待办数据与 API → 所见即所得及正文文件 → 偏好、常用词、引用与折叠 → 三驱动迁移与前后端、浏览器验证。

### 编辑器依赖方案（待批准）

- 当前 CodeMirror 面向源码编辑，现有依赖中没有所见即所得编辑框架。
- 拟用 Tiptap 3 / ProseMirror 替换正文编辑核心，使用 React 接入、基础格式/列表/历史记录，以及图片、任务列表、高亮等开源扩展；文件与笔记卡片编写项目内扩展。
- 保留现有 React/UI、上传、保存与服务端接口的可复用部分，不引入付费组件或云端编辑服务。
- 历史 Markdown 的兼容转换、未知内容保留和再次保存不丢数据需要专门验证；Markdown 插件官方当前标记为 beta，不默认假定转换无损。选定依赖后再确定序列化实现。
- 引入这一编辑器框架属于核心依赖变更，遵循 AGENTS.md 的“Ask before adding heavy dependencies”要求，在安装前取得明确确认。

## 实现结果

已建立隔离工作目录、核对现有依赖并形成实施安排；尚未修改功能代码，未部署。

## 验证结果

- 2026-09-16 只读核对本地源码。普通编辑模式的格式工具栏默认隐藏，聚焦模式显示；“已有按钮”仅指源码有实现，不能推断用户线上可见。
- 当前编辑器具有 Markdown 语法样式：加粗、斜体、标题等会实时变化，但保留 Markdown 标记；没有完整的正文媒体实时渲染。
- 文件粘贴进入附件状态；已有笔记关联功能不等同于光标位置的引用卡片。
- 未运行应用或核验线上界面；本记录不是浏览器或真机验收结果。
- 根据用户提供的待办截图核对需求：一张无标题卡片内含时间、0/2 完成计数，以及“吃饭”“打扫”两个勾选事项；没有要求复制截图配色或边框。

## 后续事项

确认编辑器核心依赖后进入实现；历史数据兼容按保留原始内容设计并记录实际验证结果。
