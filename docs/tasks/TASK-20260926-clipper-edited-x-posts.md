# TASK-20260926-clipper-edited-x-posts：编辑过的 X 帖子剪藏兼容

- 记录日期：2026-09-26
- 模块与关键词：Web Clipper、X、编辑历史、时间链接
- 维护归属：本会话，分支 `fix/clipper-edited-x-posts`
- 关联任务：[独立发帖展示](TASK-20260926-pickup-standalone-post.md)

## 背景与目标

用户的 X 帖子正文已完整加载，扩展仍提示未找到正文。通过当前浏览器页面只读检查，确认编辑过的帖子时间链接使用 `/用户/status/编号/history`，提取器原先拒绝该格式，导致整条帖子无法识别。用户要求兼容修复。

## 实现结果

- 页面内部的帖子时间链接与引用来源支持 `/history`，输出统一为普通原帖地址。
- 当前帖、上文和引用帖复用同一识别逻辑，Star 与 Pick up 均覆盖。
- 页面入口仍要求帖子详情页，不把编辑历史列表当作当前帖，避免混入旧版本正文。
- 仅添加扩展 patch Changeset，不调整权限、依赖或发布流程。

## 验证事实与边界

2026-09-26，基线 `8fe616f3`：

- 扩展 lint、38 文件共 456 项测试、类型检查、生产构建通过。
- worker 实际启动检查通过；Python 打包测试 24 通过、1 因文件系统大小写能力跳过。
- 新增回归覆盖两种剪藏模式、编辑过的上文和引用、来源地址归一化、历史页拒绝。
- 浏览器现场证据来自修复前的只读 DOM 检查；回归夹具使用合成内容，不提交用户帖子正文。未在用户浏览器安装新版或向线上 Memos 写入测试笔记。
